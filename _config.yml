title: Tri Nhu Do    
description : "Đỗ Trí Như 杜智如"

author:
  name: Tri Nhu Do
  email: tri-nhu.do@polymtl.ca

# The `>` after `description:` means to ignore line-breaks until next key.
# If you want to omit the line-break after the end of text, use `>-` instead.
#description: >


# Build settings

theme: minima

plugins:
  - jekyll-feed
  - jekyll-seo-tag

# Theme-specific settings

#minima:
  # Minima date format.
  # Refer to https://shopify.github.io/liquid/filters/date/ if you want to customize this.
  #
  # date_format: "%b %-d, %Y"

  # Generate social links in footer.
  #
#   social_links:
#     - { platform: flickr,         user_url: "https://www.flickr.com/people/tri-nhu-do/" }
#     - { platform: github,         user_url: "https://github.com/trinhudo" }
#     - { platform: google_scholar, user_url: "https://scholar.google.com/citations?user=cwdP-oYAAAAJ&hl=en" }
#     - { platform: linkedin,       user_url: "https://www.linkedin.com/in/tri-nhu-do" }

# If you want to link only specific pages in your header, uncomment this and add the path to the pages in
# order as they should show up.
#
header_pages:
   - about.md
   - publications.md
   - research.md

# Set to `true` to show excerpts on the homepage.
#
# show_excerpts: false
